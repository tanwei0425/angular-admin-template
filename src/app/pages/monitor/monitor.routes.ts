import { Routes } from '@angular/router';
import { MonitorComponent } from './monitor.component';

export const MONITOR_ROUTES: Routes = [
  { path: '', title: '自定义路由', component: MonitorComponent },
];
