import { Routes } from '@angular/router';
import { BasicFormComponent } from './basicForm.component';

export const BASIC_FORM_ROUTES: Routes = [
  { path: '', title: '自定义路由', component: BasicFormComponent },
];
